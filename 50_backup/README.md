# 로컬 PC 백업 보관함

서버에서 받은 Postgres 덤프(`.dump`)를 **이 PC에만** 두는 폴더입니다.

- 기본 경로 (Windows): `E:\personal-app\50_backup`
- **git에 `.dump` 파일을 올리지 마세요** (이 폴더의 `.gitignore`가 막습니다)
- 암호 걸린 디스크/BitLocker 권장

받는 방법: `10_docs/DB_백업.md` 의 **PC에서 받기 (Windows)** 절.
