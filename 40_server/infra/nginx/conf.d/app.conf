# Generated by scripts/render-nginx.sh — do not edit by hand.
# Source templates live in nginx/templates/.
# Keep a bootstrap HTTP stub checked in so `docker compose up` works before first render.

server {
    listen 80;
    server_name myfamilyhub.duckdns.org;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        default_type text/plain;
        return 200 'MyFamily Hub server is up (myfamilyhub.duckdns.org). App container not deployed yet.\n';
    }
}
