# MyFamily Hub — 작업 내용 로그

이 문서는 **지금까지 끝난 일 / 현재 상태 / 다음 할 일**의 기준 문서입니다.  
에이전트·사람은 작업 전후로 이 파일을 읽고, 의미 있는 진척이 있으면 **여기에 이어서 기록**합니다.

최종 갱신: 2026-08-11  
작업 브랜치: `cursor/continue-latest-mockup-69de` (PR #2 → `main`)  
기준 원격 진척: `cursor/oracle-cloud-infra-docs` 목업 위에 이어서 개발

---

## 1. 제품 목표 (요약)

- **이름(가칭):** MyFamily Hub
- **형태:** iOS Safari PWA (홈 화면 추가)
- **핵심:** 가족(2~3인) 자산·구독·증명서·사진·캘린더 통합 + 항목별 `is_shared`
- **운영비 목표:** Oracle Always Free + 월 0원
- **상세 기획:** `10_docs/기획서.md`, `10_docs/프롬프트.md`

---

## 2. 레포 구조

| 경로 | 역할 |
| --- | --- |
| `10_docs/` | 기획·인프라·API·**이 작업 로그** |
| `20_client/` | React 18 + Vite + TS + Tailwind 목업 UI |
| `30_data/` | 로컬 JSON (스타터 태스크 API용) |
| `40_server/` | Express API + Prisma + infra 스크립트 |
| `.cursor/` | Cloud Agent `environment.json` + **project rules** |
| `AGENTS.md` | 모든 코딩 에이전트용 공통 지침 |

npm workspaces: `20_client`, `40_server` (패키지명 `@personal-app/client`, `@personal-app/server`).

---

## 3. 완료된 작업

### 3.1 기반 / 환경
- [x] 풀스택 스타터(태스크 CRUD) + Cloud Agent 환경
- [x] 폴더 번호 접두사 정리 후 **workspaces 경로 수정** (`client`/`server` → `20_client`/`40_server`)
- [x] 최신 목업 브랜치(`cursor/oracle-cloud-infra-docs`)를 작업 기준으로 설정

### 3.2 프론트 목업 (이전 에이전트 + 유지)
- [x] 화면: 로그인, 대시보드, 자산, 증명서, 구독, 캘린더, 사진, 설정
- [x] ko/ja i18n, 표시 통화 Context, 개인/가족 Scope 토글
- [x] OCI 호스트 nginx에 정적 빌드 배포됨

### 3.3 인프라 (부분 완료)
- [x] OCI 인스턴스: Public IP **`129.225.196.226`** (Oracle / Osaka)
- [x] HTTP 목업: `http://129.225.196.226`, `http://sumicchogurashi.duckdns.org`
- [x] DuckDNS 계정·도메인 생성 및 IP 연결 (`sumicchogurashi.duckdns.org`)
- [x] **prod / dev 이중 배포 설계** — `sumicchogurashi-dev` + host nginx 템플릿 (`10_docs/prod_dev_이중배포.md`)
- [ ] DuckDNS에 `sumicchogurashi-dev` 생성 + 서버 nginx 적용 (민호 서버 작업)
- [x] 인프라 스크립트/체크리스트: `40_server/infra/`, `10_docs/인프라_1단계_체크리스트.md`
- [ ] HTTPS (Let's Encrypt) — **아이폰만 사용 중이라 서버 작업 보류**
- [ ] 프로덕션에 API/Postgres 컨테이너 배포

### 3.4 백엔드 / DB
- [x] Prisma 스키마 + 초기 마이그레이션 (`40_server/prisma/`)
  - families, users, assets, documents, subscriptions, transactions, photos, calendar_events
- [x] 로컬 Postgres compose: `40_server/docker-compose.dev.yml`
- [x] 문서: `10_docs/DB_스키마.md`
- [x] 인증·가족 API (JWT)
  - `POST /api/auth/register|login`, `GET|PATCH /api/auth/me`
  - `GET /api/family`, `POST /api/family/join`, `POST /api/family/invite/rotate`
- [x] 테스트: 메모리 `AuthRepository` 포함 서버 테스트 통과
- [x] 문서: `10_docs/인증_가족_API.md`
- [x] 로컬/Cloud Agent: `MEMORY_AUTH=1` 로 Postgres 없이 인증 UI 테스트 가능

### 3.5 클라이언트 연동
- [x] 로그인/가입 → 실 API, JWT `localStorage`
- [x] 보호 라우트 (`RequireAuth`)
- [x] 설정: 실사용자·가족·초대코드·로그아웃
- [x] 대시보드 인사/가족명에 로그인 사용자 반영
- [ ] 자산/증명서/캘린더/사진 — **아직 mock 데이터**
- [x] **구독** — CRUD API + 구독 화면 실데이터 연동 (로컬 `MEMORY_AUTH=1`)
- [x] **Passkey(A안)** — WebAuthn 로그인/가입, 1회용 초대 토큰, 가입 폐쇄

---

## 4. 현재 실행 방법

```bash
npm install
MEMORY_AUTH=1 npm run dev   # 또는 npm run dev (dev:server 가 MEMORY_AUTH=1)
# 웹: http://localhost:5173  → 먼저 /login 에서 가입
```

실 Postgres:

```bash
cd 40_server
docker compose -f docker-compose.dev.yml up -d
cp .env.example .env   # MEMORY_AUTH 없이 실행
npm run db:migrate
npm run dev            # workspace server, MEMORY_AUTH 끄고
```

공개 HTTP 목업(정적, API 없음): `http://sumicchogurashi.duckdns.org`  
개발 미리보기(설정 후): `http://sumicchogurashi-dev.duckdns.org` — 절차는 `10_docs/prod_dev_이중배포.md`

---

## 5. 운영자 제약 (중요)

- 메인 작업 환경: **아이폰** (PC 상시 사용 불가 가정)
- OCI SSH 키·`90_secret`은 Cloud Agent에 없음 → 서버 직접 작업은 사용자 OCI 콘솔/Run command 또는 PC 필요
- DuckDNS **token**, JWT secret, DB 비밀번호는 레포/채팅에 올리지 말 것
- 도메인/브랜드명은 임시(`sumicchogurashi.duckdns.org`, MyFamily Hub 가칭) — 나중에 교체 가능

---

## 6. 다음에 할 일 (우선순위)

1. DuckDNS `sumicchogurashi-dev` 추가 → host nginx prod/dev 적용 → `deploy-static.sh dev`
2. HTTPS (certbot) + `WEBAUTHN_RP_ID` (아이폰 Face ID 실기)
3. **자산 API + 목업 자산 화면 실데이터 연동**
4. Prisma/Postgres를 OCI에 배포 (`db:migrate`)
5. 증명서 / 캘린더 / 사진 / PWA 완성

---

## 7. 에이전트 기록 규칙

의미 있는 작업이 끝나면 이 파일에:

1. **날짜**
2. **한 일** (체크박스 갱신)
3. **남은 일 / 막힌 점**
4. **관련 PR·커밋·URL**

을 짧게 추가한다. 상세 설계는 별도 문서(`DB_스키마.md` 등)에 두고, 여기는 **현황판**으로 유지한다.

### 변경 이력

| 날짜 | 요약 |
| --- | --- |
| 2026-08-11 | prod/dev 이중 배포: DuckDNS 2도메인 + host nginx 템플릿·배포 스크립트 |
| 2026-08-11 | Passkey(WebAuthn) A안: 가입 폐쇄, 1회 초대 토큰, Face ID 로그인 UI |
| 2026-08-11 | 구독 API(CRUD, scope 필터, 가족 공유) + SubscriptionsPage 실데이터·추가 폼 연동 |
