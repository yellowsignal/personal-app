{
  "name": "personal-app",
  "private": true,
  "version": "0.1.0",
  "description": "MyFamily Hub — React + Vite client, Express + TypeScript API.",
  "workspaces": [
    "20_client",
    "40_server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "MEMORY_AUTH=1 npm run dev --workspace @personal-app/server",
    "dev:client": "npm run dev --workspace @personal-app/client",
    "build": "npm run build --workspace @personal-app/server && npm run build --workspace @personal-app/client",
    "lint": "npm run lint --workspace @personal-app/server && npm run lint --workspace @personal-app/client",
    "typecheck": "npm run typecheck --workspace @personal-app/server && npm run typecheck --workspace @personal-app/client",
    "test": "npm run test --workspace @personal-app/server && npm run test --workspace @personal-app/client",
    "db:generate": "npm run db:generate --workspace @personal-app/server",
    "db:migrate": "npm run db:migrate --workspace @personal-app/server"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
